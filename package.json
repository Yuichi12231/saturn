{
  "name": "saturn",
  "private": true,
  "workspaces": [
    "app",
    "ai-agent"
  ],
  "scripts": {
    "start": "cd app && npm run dev",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "agent": "cd ai-agent && npm run start",
    "deploy:app": "cd app && npm run build",
    "deploy:agent": "cd ai-agent && npm run start"
  }
}
